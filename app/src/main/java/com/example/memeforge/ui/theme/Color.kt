package com.example.memeforge.ui.theme

import androidx.compose.ui.graphics.Color

val Primary = Color(0xFF424242)  // Dark gray for primary actions
val Background = Color(0xFFFAFAFA)  // Light background
val Surface = Color(0xFFFFFFFF)    // White surface
val InputBackground = Color(0xFFF0F0F0)  // Slightly lighter gray for inputs
val TextPrimary = Color(0xFF212121)  // Dark gray for primary text
val TextSecondary = Color(0xFF616161)  // Refined medium gray
val Divider = Color(0xFFD6D6D6)  // Softer divider

// Dark Theme Colors
val DarkBackground = Color(0xFF121212)  // Dark background
val DarkSurface = Color(0xFF1E1E1E)    // Slightly lighter for surface
val DarkInputBackground = Color(0xFF1F1F1F)  // More consistent with surface
val DarkTextPrimary = Color(0xFFEEEEEE)  // Light gray for primary text
val DarkTextSecondary = Color(0xFF9E9E9E)  // Medium gray for secondary text
val DarkDivider = Color(0xFF3A3A3A)  // Dark gray for dividers
